<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.dom.project.mapper.EquipmentMoveoutMapper">

    <insert id="insert" parameterType="com.dom.project.entity.EquipmentMoveout">
        INSERT INTO equipment_moveout (moveout_id, residence_history_id, equipment_id, disposition, processed_at, remarks, created_by)
        VALUES (#{moveoutId}, #{residenceHistoryId}, #{equipmentId}, #{disposition}, #{processedAt}, #{remarks}, #{createdBy})
    </insert>
</mapper>
