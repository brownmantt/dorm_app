{
  "name": "dorm-app",
  "private": true,
  "scripts": {
    "build": "npm ci --prefix dom-dev && npm run build --prefix dom-dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.105.0"
  }
}
