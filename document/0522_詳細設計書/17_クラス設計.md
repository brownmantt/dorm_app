# 11. クラス設計（代表）

> 親文書: [README.md](./README.md)  
> 各モジュール設計書にも抜粋記載。

## 11.1 `DormAllocationService`

詳細: [20_寮割カレンダー.md](./20_寮割カレンダー.md#クラス設計)

```text
+ buildCalendar(DormAllocationQuery): DormAllocationCalendarVO
+ isOccupiedOnDate(history, date): boolean
+ detectRoomConflicts(blocks): Map<roomId, Set<date>>
- filterVisibleResidents(histories, yearMonth): List<ResidenceHistory>
```

## 11.2 `ResidenceHistoryService`

詳細: [08_入退寮管理.md](./08_入退寮管理.md#クラス設計)

```text
+ register(ResidenceRegisterCommand): ResidenceResult
+ checkout(String historyId, CheckoutCommand): void
+ validate(ResidenceRegisterCommand): ValidationResult
- assertNoOverlap(roomId, period)
- assertGenderMatch(employeeId, dormitoryId)
- assertDormCapacity(dormitoryId, period)
- saveChangeLog(changeType, before, after)
```

## 11.3 `DormFeeCalculationService`

詳細: [09_寮費管理.md](./09_寮費管理.md#クラス設計)

```text
+ calculate(FeeCalculationQuery): FeeCalculationResult
- billableDays(period, yearMonth): int
- resolveDailyRate(room): BigDecimal
```

## 11.4 `DormitoryManagerService`

詳細: [21_責任者・所属管理.md](./21_責任者・所属管理.md)

```text
+ assign(dormitoryId, AssignManagerCommand): void
+ remove(dormitoryId): void
+ findByDormitory(dormitoryId): ManagerVO
- assertCurrentResident(residenceHistoryId)
```

## 11.5 `PostalCodeService`

詳細: [07_寮・部屋管理.md](./07_寮・部屋管理.md#業務ロジック)

```text
+ lookupAddress(String postalCode): AddressLookupVO
- normalizePostalCode(String postalCode): String
```

## 11.6 `RegionService` / `UsageTypeService` / `UnitPriceService`

詳細: [23_マスタ管理（地域・利用形態・単価）.md](./23_マスタ管理（地域・利用形態・単価）.md#クラス設計)

```text
RegionServiceImpl — 地域マスタ CRUD、コード重複チェック
UsageTypeServiceImpl — 利用形態マスタ CRUD
UnitPriceServiceImpl — 単価 CRUD、参照整合検証、コード自動採番（UC）、maxUsageDays 未指定時 -1
```

## 11.7 `VacancyService`

詳細: [11_空き室管理.md](./11_空き室管理.md#クラス設計)

```text
+ listVacancies(VacancyQuery): List<VacancyDto>
+ findAssignable(AssignableRoomQuery): List<RoomDto>
- isVacant(roomId, LocalDate): boolean
- hasFutureReservation(roomId, LocalDate): boolean
```
